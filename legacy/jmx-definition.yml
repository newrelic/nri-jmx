name: com.newrelic.jmx
description: Reports status and metrics for jmx service. Legacy file kept for compatibility.
protocol_version: 2
os: linux

commands:
  all_data:
    command:
      - ./bin/nri-jmx
    interval: 15
